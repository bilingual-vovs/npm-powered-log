const styles = require('./as')

function typeOf(value){
    let type = typeof value

    switch (typeof value){
        case "string":
            let isStyle = false

            
        break;
    }
}